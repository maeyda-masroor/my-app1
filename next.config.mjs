const nextConfig = {
    images: {
      remotePatterns: [
        {
          protocol: 'https',
          hostname: 'media.geeksforgeeks.org',
        },
      ],
    }
  };
  
  export default nextConfig;